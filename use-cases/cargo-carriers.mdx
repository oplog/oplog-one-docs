---
title: 'Cargo Carriers'
description: 'Manage cargo and shipping companies using OPLOG Cargo Carriers API'
---

OPLOG's [Cargo Carriers API](../api-reference/cargo-carriers) provides comprehensive management capabilities for cargo and shipping companies integrated with the platform. This guide covers the essential requirements, query methods, and examples for working with cargo carrier data.

## Integration Overview

The Cargo Carriers API is designed to handle shipping partner management:
- **Carrier Information** - Access shipping company details and capabilities
- **Dynamic Updates** - Real-time carrier availability and service updates
- **Reference Management** - Consistent carrier identification across systems
- **Service Integration** - Connect with various shipping and logistics providers

<Info>
All cargo carrier operations use GET endpoints to retrieve current carrier information. The system maintains up-to-date carrier data and provides query capabilities for integration purposes.
</Info>

## What are CargoCarriers?

**Definition**: Defines the cargo/shipping companies that OPLOG works with. Requests should be sent or read using these definitions.

CargoCarriers represent the logistics partners and shipping companies integrated with the OPLOG platform. These entities are essential for managing shipments, tracking deliveries, and coordinating logistics operations.

<Warning>
**Important Notes**

Since cargo companies can be updated, please make an API request for the current response to ensure you have the latest information.
</Warning>

## API Usage

### Basic Carrier Query

Retrieve all available cargo carriers:

```bash
GET /openapi/v1/CargoCarriers
```

<Accordion title="CargoCarriers API Response">
```json
{
  "items": [
    {
      "key": "CARGO_CARRIER_001",
      "value": "Express Logistics Ltd."
    },
    {
      "key": "CARGO_CARRIER_002", 
      "value": "Global Freight Solutions"
    },
    {
      "key": "CARGO_CARRIER_003",
      "value": "Local Delivery Co."
    }
  ]
}
```
</Accordion>

## Response Fields

<CardGroup cols={2}>
  <Card title="key" icon="key">
    **String** - Unique identifier for the cargo carrier. Use this value when making API requests.
  </Card>
  <Card title="value" icon="tag">
    **String** - Human-readable display name of the cargo carrier for UI purposes.
  </Card>
</CardGroup>

## Best Practices

<CardGroup cols={2}>
  <Card title="Cache the Response" icon="database">
    Cache cargo carrier data locally to minimize API calls and improve application performance. Update cache periodically based on your business needs.
  </Card>
  <Card title="Use Key Values" icon="key">
    Always use the `key` field when making API requests or storing references. Keys remain consistent while display values may change.
  </Card>
  <Card title="Display Values" icon="eye">
    Use the `value` field for user-facing displays, labels, and dropdown options. This provides human-readable carrier names.
  </Card>
  <Card title="Regular Updates" icon="refresh">
    Refresh cargo carrier data regularly as new carriers may be added or existing ones updated. Consider implementing automated refresh mechanisms.
  </Card>
</CardGroup>

## Implementation Examples

<AccordionGroup>
  <Accordion icon="code" title="JavaScript Integration">
    ```javascript
    // Fetch and cache cargo carriers
    async function fetchCargoCarriers() {
      try {
        const response = await fetch('/openapi/v1/CargoCarriers', {
          headers: {
            'Authorization': 'Bearer YOUR_TOKEN',
            'Content-Type': 'application/json'
          }
        });
        
        const data = await response.json();
        
        // Cache the carriers
        localStorage.setItem('cargoCarriers', JSON.stringify(data.items));
        localStorage.setItem('carriersLastUpdate', Date.now().toString());
        
        return data.items;
      } catch (error) {
        console.error('Failed to fetch cargo carriers:', error);
        return [];
      }
    }
    
    // Create dropdown options
    function createCarrierDropdown(carriers) {
      return carriers.map(carrier => ({
        label: carrier.value,    // Display name
        value: carrier.key       // Use for API requests
      }));
    }
    ```
  </Accordion>
  
  <Accordion icon="code" title="Caching Strategy">
    ```javascript
    // Check if carriers need refresh (daily update)
    function needsCarrierRefresh() {
      const lastUpdate = localStorage.getItem('carriersLastUpdate');
      if (!lastUpdate) return true;
      
      const oneDayAgo = Date.now() - (24 * 60 * 60 * 1000);
      return parseInt(lastUpdate) < oneDayAgo;
    }
    
    // Get carriers with automatic refresh
    async function getCargoCarriers() {
      if (needsCarrierRefresh()) {
        return await fetchCargoCarriers();
      }
      
      const cached = localStorage.getItem('cargoCarriers');
      return cached ? JSON.parse(cached) : await fetchCargoCarriers();
    }
    ```
  </Accordion>
  
  <Accordion icon="shipping-fast" title="Order Integration">
    ```javascript
    // Use carrier key when creating orders
    async function createOrderWithCarrier(orderData, carrierKey) {
      const orderPayload = {
        ...orderData,
        cargo: {
          cargoType: "RegularDelivery",
          cargoCarrier: carrierKey  // Use the carrier key, not display name
        }
      };
      
      const response = await fetch('/openapi/v1/SalesOrders', {
        method: 'POST',
        headers: {
          'Authorization': 'Bearer YOUR_TOKEN',
          'Content-Type': 'application/json'
        },
        body: JSON.stringify(orderPayload)
      });
      
      return response.json();
    }
    ```
  </Accordion>
</AccordionGroup>

## Common Use Cases

<AccordionGroup>
  <Accordion icon="list" title="Dropdown Population">
    Populate shipping carrier selection in order forms and shipping interfaces.
  </Accordion>

  <Accordion icon="search" title="Carrier Lookup">
    Find carrier information when processing orders or tracking shipments.
  </Accordion>

  <Accordion icon="sync" title="System Integration">
    Synchronize carrier data across multiple systems and applications.
  </Accordion>

  <Accordion icon="chart-line" title="Analytics Integration">
    Include carrier information in reporting and analytics systems.
  </Accordion>
</AccordionGroup>

---

## TAG Management

OPLOG.ONE's TAG management system provides flexible metadata tagging capabilities across various business entities. Organizations can attach custom labels to records, transforming basic data into actionable intelligence for better workflow optimization.

The system proves valuable for both operational efficiency and analytics. Teams can quickly identify and process records based on specific criteria - staff can locate items requiring special handling, managers can prioritize high-value transactions, and leadership can analyze performance metrics across different categories. This flexible approach eliminates rigid classification systems, allowing businesses to evolve their tagging strategies as operational needs change.

### TAG Best Practices for Cargo Carriers

<CardGroup cols={2}>
  <Card title="Service Type" icon="truck">
    Tag by service: `express`, `standard`, `freight`, `same-day`
  </Card>
  <Card title="Coverage Area" icon="globe">
    Mark coverage: `domestic`, `international`, `regional`, `global`
  </Card>
  <Card title="Reliability" icon="shield-check">
    Rate performance: `premium`, `reliable`, `budget`, `new-partner`
  </Card>
  <Card title="Special Services" icon="star">
    Note capabilities: `fragile-handling`, `cold-chain`, `oversized`
  </Card>
</CardGroup>

**TAG Usage Examples:**
- **Service Tags**: `express-delivery,standard-shipping,freight-service,same-day`
- **Coverage Tags**: `domestic-only,international,regional,nationwide,global`
- **Performance Tags**: `premium-carrier,reliable,budget-option,new-partner`
- **Capability Tags**: `fragile-handling,cold-chain,oversized-items,white-glove`
- **Integration Tags**: `api-enabled,tracking-available,real-time-updates`

<Info>
TAG management for cargo carriers enables better carrier selection, performance tracking, and logistics optimization based on specific shipping requirements.
</Info>